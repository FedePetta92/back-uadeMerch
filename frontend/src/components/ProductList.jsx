// props: productos (array de ProductoResponseDTO)
function ProductList({ productos }) {
  return (
    <div className="border p-2 my-2">
      TODO: ProductList — {productos.length} productos recibidos
    </div>
  )
}

export default ProductList
