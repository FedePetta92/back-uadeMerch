// props: categorias (array de { id, nombre })
function CategoryList({ categorias }) {
  return (
    <div className="border p-2 my-2">
      TODO: CategoryList — {categorias.length} categorías recibidas
    </div>
  )
}

export default CategoryList
