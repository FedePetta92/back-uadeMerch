// props: producto ({ id, nombre, descripcion, precio, stock, categoriaId, imagen })
function ProductCard({ producto }) {
  return (
    <div className="border p-2 my-2">
      TODO: ProductCard — {producto.nombre}
    </div>
  )
}

export default ProductCard
